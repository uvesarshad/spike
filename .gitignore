node_modules/
dist/
spike.config.json
qa.config.json
artifacts/
generated-tests/
.spike/
.spike-action-cache/
.qa-action-cache/
*.log
*.tsbuildinfo

# spike runtime debris (frozen reference code stays, its output doesn't)
spikes/.chrome-profile/
spikes/**/node_modules/
spikes/**/shots/
# throwaway lite-mode Phase-0 spike — holds a live API key at runtime, never commit
spikes/lite-mode/
.DS_Store
