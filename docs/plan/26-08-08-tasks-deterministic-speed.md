> Source audit: [26-08-08-audit-deterministic-speed](./26-08-08-audit-deterministic-speed.md)
> Updated: 26-08-09 · 53/54 done

- [x] **(A23, P0)** Build the app model + coverage ledger: a discovery pass enumerating reachable routes/states (crawl + link/form extraction, seeded from the app's router manifest where available), persisted as an app model, plus a coverage ledger (routes seen/exercised, elements touched, flows generated) the planner queries to pick what to test next. Entry points: `spike map <url>`, `spike coverage`. Without this, "autonomous full-project QA" is a human writing 200 task strings.
  - **DONE 26-08-09.** `src/discovery/` — static route extraction (Next app/pages, sitemap, robots) → deterministic same-origin BFS crawl (depth/page caps, parameterised-URL collapse) → documented seam for AI exploration of interaction-gated state. `.spike/app-model.json` ledger with route + interactive-element coverage. **Design correction by the agent:** a literal port of `stableAxMaterial` minus name/value would NOT collapse a 10-vs-11 item list (one extra line still changes the hash), so the structural signature hashes the SET of unique `depth|role` pairs — sibling count collapses, a new `dialog` subtree does not. 76/76 (v47).
- [x] **(A24, P0)** Implement the invariant oracle tier: assertions requiring zero human input, run on every step of every flow — no uncaught page errors, no 4xx/5xx (pairs with A18), no layout overflow, no broken images, no empty required regions, optional a11y and response-budget checks. Then layer a differential tier (baseline store, shared with A11) and metamorphic relations (cart count, sort membership, pagination).
  - **Tiers 1+2 DONE 26-08-09.** Differential: AX-structural diff that groups siblings by role so pure count churn never false-positives while a new region does; network-shape diff over `failed`/`clientError`; masks; `.spike/baselines/`; and `compareEnvironments()` as a first-class peer of `compareToBaseline()` — environment-vs-environment needs no blessing, which is the escape from the blessing problem. Metamorphic: count-delta/sort/filter/pagination relations + deterministic pattern detection, AI-proposal seam documented but NOT called. 46/46 (v48). Agent's honest split: set/count relations are exact; `login→logout→login` and `same-url-twice` are speculative since "same state" is only as precise as the caller's state blob.
  - **Tier 0 IN PROGRESS 26-08-09.** `src/assertions/invariants.ts` built (7 rules incl. the high-value `rendered-undefined`), probe wired through a narrow `BrowserPort.probeInvariants()` (one compile-time-constant evaluation — never model-supplied JS) and `CdpBrowser`, called at all 3 drain sites in `loop.ts`. Violations are recorded as evidence and are deliberately NON-FATAL until dogfooded. 46/46 (v35). First live run caught the fixture's real `page-error` + `network-error` on exactly the failing step. **Calibration from that run:** `empty-required-region` was firing on all 7 steps at `error` severity, drowning the 2 real signals 12-to-2 — demoted to `warn` (an empty `<main>` stays `error`). Remaining: differential tier (baseline store, shares A11) and metamorphic relations.
- [x] **(A1, P0)** Exercise the deterministic pillar end-to-end for the first time: run the fixture app with `readOnly` off (`SPIKE_READ_ONLY=0`), get a genuine `pass`, confirm `generated-tests/<slug>.json` + `.spec.ts` are written, then `spike replay` the script and confirm a $0 zero-planner verdict. Commit the resulting script as this repo's own regression suite.
  - **DONE 26-08-09.** Root cause was `readOnly: true` (config.ts:135) silently no-op'ing every action in BOTH e2e suites, which had therefore been failing/dying since that default landed. Fixed in `test/e2e.run-fixture.ts` and `test/e2e.recorder.ts` (fixtures they start themselves — opting out is the point). Results: `test:e2e` **6/6**, `e2e.recorder` **19/19**, first genuine `pass`, first script ever written to `generated-tests/` + Playwright twin. Measured: fresh AI pass 8 steps / **81s** vs deterministic replay **5s, zero planner calls** (~16×).
- [x] **(A2, P0)** Attack the 5.4s-per-3-actions model-latency floor: make deterministic-first the default execution order (cache → script → replay → navigator), so a model call only happens when no deterministic source can supply the next action. (Depends on A9 tightening; pairs with enhancement items 5–6.)
  - **DONE 26-08-09 (by composition, no new machinery).** The deterministic-first order already exists once its dependencies landed: (1) the replay matcher short-circuits an entire run before any model call (`engine.ts` `qaRun`), (2) the verified action cache is consulted before EVERY navigator call (`loop.ts:764`) and only defers to the model on a miss — active by default since A9 tightened its verification, (3) the navigator runs only when neither supplies an action. The `script` tier in the original wording is model-EMITTED by design (`script-runner`), so it cannot precede the model and is not part of this ordering. Verified in the wild: `e2e.recorder` shows a matched replay finishing in 5s with zero planner calls vs 81-104s for the equivalent AI pass.
- [x] **(A3, P0)** Remove the concurrency singletons using `PlaywrightBrowser`'s per-run `BrowserContext` (verified isolated for cookies + localStorage) instead of one shared profile: one context per worker, `--workers N` on `replay --all`, and a per-run artifacts dir. Still needed alongside: the Nano runner becomes per-session or a properly locked shared service (it is a separate HTTP port + tab, outside Playwright's contexts), and the VibeService single-run lock (`vibe/service.ts:94,119`) stays deliberate for the panel.
  - **DONE 26-08-09.** `via: 'playwright'` gives each worker its own `BrowserContext` on ONE shared Chrome (the cheap path); other transports get `allocateIsolatedSession()` (free port + fresh profile). Also fixed two collision points the audit only described: the Nano runner HTTP server is now a refcounted per-port singleton (kills the literal `EADDRINUSE`), and `ensureChrome()` dedupes concurrent launches on the same port. `withNanoLock()` serialises the shared Nano tab. 52/52 (v49).
- [x] **(A4, P0)** Add `waitFor`/actionability + `waitForIdle` primitives to `BrowserPort` and delete every fixed `sleep()` in `cdp-browser.ts` (~20 sites) and `replay.ts:215`. `Network.enable` is already on (`console-network.ts:22`) so network-idle needs no new CDP domain. Target: replace ~1,150ms of fixed sleep per 3-action batch with condition-based waits.
  - **DONE 26-08-09.** `waitForIdle`/`waitForActionable` added to `BrowserPort` (both optional), `createNetworkIdleTracker` shared by CDP + extension transports, ~14 fixed sleeps in `cdp-browser.ts` replaced with condition-based waits, `replay.ts` now gates actions on actionability. 15/15 (v38). **Coordinator fixed two bugs in this work:** (1) `waitForIdle` measured quiet from the last NETWORK event, which on an already-idle page was satisfied on entry — so it returned after the 50ms floor, before a click's XHR was even issued; now anchored to `max(lastActivity, waitStart)`. (2) `verifyTyped` read once instead of polling, so the shortened settle made it see a stale value and double-type via the fallback; now polls to 600ms.
- [x] **(A5, P0)** Build a real assertion vocabulary alongside substring `assert_dom`: `assert_text {exact|contains|regex}`, `assert_count`, `assert_url`, `assert_attr`, `assert_network {urlPattern, status}`, `assert_no_console_errors`, `assert_visible/hidden/enabled/disabled` — wired through actions.ts schemas, the loop executor, the recorder, and replay.
  - **DONE 26-08-09.** `src/assertions/dom-assertions.ts` — pure evaluator for `assert_text` (exact/contains/regex), `assert_count`, `assert_url`, `assert_state`, `assert_network`, `assert_no_console_errors`; added to the `Action` union + flat `PLAN_JSON_SCHEMA`. Coordinator wired the evaluator into the loop executor and fixed the exhaustive-switch fallout in 4 files the agent could not reach (`action-cache.ts` rejects the new read-only verbs from caching, `loop.ts` stepKind/humanizeAction, `report.ts`, `fix-prompt.ts`). 55/55 (v42). The audit's motivating case works: `contains "Order"` still passes "Order failed", `exact` fails it. `assert_dom` untouched.
- [x] **(A26, P1)** ~~Decide the Core-mode engine~~ — **RESOLVED 2026-08-08.** Spike (`docs/plan/spikes/a26-connect-over-cdp.mjs`) passed 9/9: `newContext()` over `connectOverCDP` gives real cookie + localStorage isolation, concurrent driving, clean `storageState()`, and working `route()`, without disturbing the daemon's Chrome. **Decision: A3/A6/A13 via Playwright `connectOverCDP` as a `PlaywrightBrowser` implementation of `BrowserPort`; A4 hand-built in the port layer so lite mode gets it too.** `playwright-core` = 1 package, no browser download.
- [x] **(A26, P1)** Implement `PlaywrightBrowser` — a third `BrowserPort` implementation attaching via `chromium.connectOverCDP()` to the Chrome the daemon already launches (Core mode only; lite keeps `ExtensionBrowser`). Add `playwright-core` as a dependency. This is the vehicle for A3, A6, and A13 below — do it before them.
  - **DONE 26-08-09.** `src/ports/playwright-browser.ts` — attaches via `connectOverCDP` to the daemon's Chrome (never launches its own). A `cdpClientFromSession()` Proxy adapts Playwright's `CDPSession` to the `chrome-remote-interface` shape so `snapshotAxTree`/`attachCapture`/`setLogpointByContent` are REUSED, not reimplemented — behaviour matches `CdpBrowser` by construction. Playwright's own engine used for `waitForActionable` (`click({trial:true})`); `storageState()`/`setStorageState()`/`route()` exposed for A6/A13. `playwright-core` = 1 package, no browser download. **39/39 (v45), including the same shared `runPortContract` suite `CdpBrowser` passes**, plus two-context cookie+localStorage isolation on one Chrome.
- [x] **(A27, P1)** Make lite mode's AI split explicit and correct: change the default navigator from the no-op `nano:ondevice` pin (`settings-data.ts:73`, which `lite-engine.ts:88-91` documents as "not a plan-step candidate") to a cheap vision API model (`claude-haiku-4-5` / `gemini-3-flash-preview`, already in `NAVIGATOR_MODELS` `settings-data.ts:88-92`); keep the brain on `claude:api` Sonnet; demote Nano to an opt-in Experimental navigator; make the Settings panel display what is actually running. Panel copy states the contract: one API key, a small model drives, a big model judges.
  - **DONE 26-08-09.** Lite navigator default nano→`claude:api` (Haiku drives, Sonnet judges, one key); daemon default decoupled and left on nano. `resolveNavigatorName()` reports the adapter that will ACTUALLY lead plan-step — coordinator fixed a bug where a keyless pin still displayed itself (a Gemini-only user would have seen "claude" while gemini drove).
- [x] **(A25, P1)** Add change-triggered re-exploration: fingerprint the app model per run (route set + per-route AX signature), diff against the previous run, and trigger targeted AI authoring only for new or changed surface — so the expensive AI pass is spent exactly where determinism has nothing to replay. (Depends on A23's app model.)
  - **DONE 26-08-09.** `diffAppModel()` → new/changed/removed/unchanged buckets plus a priority order (new surface > changed-with-coverage > changed-without-coverage > removed) so a caller drains it under a budget. Pure, no model calls. Covered in v47.
- [x] **(A6, P1)** Add storage-state capture/injection via Playwright's `context.storageState()` / `newContext({storageState})` (verified working in the spike), plus a suite-level auth fixture that logs in once and injects state into every subsequent flow. Kills ~36–40 min of repeated UI login per 200-flow suite pass. Keep a raw-CDP fallback (`Network.getCookies`/`setCookies` through `cdpClient()`, `cdp-browser.ts:70-72`) for the extension transport.
  - **DONE 26-08-09.** `captureStorageState`/`injectStorageState` prefer Playwright's `storageState()`, fall back to raw CDP `Network.getCookies`/`setCookies` for cdp/extension. `--storage-state` / `--save-storage-state` on run+replay, and `replay --all --auth-fixture` runs the suite `setup` once and injects its state into every entry.
- [x] **(A7, P1)** Make headless reachable: add `--headless` for the replay path, gated on the run not needing Nano (`replay.ts:53-55`). Prerequisite: split the Nano runner onto its own port/Chrome first (slice of A3) — today whoever launches the shared Chrome first wins the headless choice and would silently drag Nano headless (`engine.ts:188-189,224`, `launch.ts:44`).
  - **DONE 26-08-09.** `headless` config + `--headless` on run/replay, default false. The prerequisite was real and is handled: `resolveNanoLaunchOpts()` splits Nano onto its OWN headed Chrome when the run is headless, so a headless replay can no longer silently drag Nano into a headless browser; `qaReplay` gates Nano on whether the script actually contains an `assert_visual`.
- [x] **(A8, P1)** Upgrade locator strategy: `data-testid`-first resolution with role+name fallback, teach the recorder to prefer testids, record a stack of candidate locators per step, and replace the ambiguity hard-fail (`replay.ts:417-422`) with scored disambiguation (position, nearest landmark, sibling text) before giving up.
  - **DONE 26-08-09.** `data-testid`/`-test-id`/`-test`/`-qa` captured into `AxNode.testId` via ONE bulk `DOM.getDocument` per snapshot (zero extra per-step round-trips); a node carrying a testid survives AX pruning even with no accessible name — the canvas/SVG case. Ordered candidate stacks (testid → role+name+nth → qaId → text) on `ScriptTarget`, and scored disambiguation (nth-closeness, nearest landmark, sibling text) that resolves only on a clear winner and still fails explicitly on a tie. Fully backward compatible: a pre-A8 script synthesises the exact old stack. 21/21 (v50), with v46/v37/v28 green.
- [x] **(A9, P1)** Tighten `verifyActionEffect`: remove the `changes.length` catch-all (`action-cache.ts:335`) and require intent-matching proof per action type (a click must produce a targeted change — target's own state, new URL, or named-region mutation — not merely "the page changed"). Then flip `actionCache` default to true (`config.ts:123`).
  - **DONE 26-08-09.** `verifyActionEffect` catch-all replaced with intent-specific proof per action type (click requires a targeted change / URL change / target disappearance / live-region change — a bare page-signature diff no longer counts); secret-`type` requires an actual value change; `select_option` prefers exact over substring; `navigate` rejects a same-URL error page. `actionCache` default flipped to **true**. 25/25 (v39); existing v28 16/16 and v31 8/8 still pass.
- [x] **(A10, P1)** Fix replay matching failure modes: surface near-miss scores at the CLI when no script clears the 0.62 threshold (`matcher.ts:29`); make the silent replay-fail→fresh-AI fallback loud (`engine.ts:429-436`), including in `--json` mode; content-hash script names to kill the 60-char slug-collision overwrite (`script.ts:82-90`, `saveScript :237-245`).
  - **DONE 26-08-09.** `matchReplayScriptDetailed()` returns the best candidate + score even below threshold; near misses within 0.15 are surfaced as a progress line; light stemming + compound aliases so `login`/`log in` tokenise alike (a real pair went 0.600 → 1.000). Silent replay-fail→fresh-AI fallback now records `replayFallback` on the report, visible in `--json`. 26/26 (v41). Verified live: the e2e printed "matched replay failed — falling back to a fresh AI run", a path that was previously invisible.
- [x] **(A11, P1)** Add flake control: per-flow `retries: N`, a `flaky` verdict when fail-then-pass on retry, a quarantine list excluded from the exit code, and a suite report separating regressions from flakes.
  - **DONE 26-08-09.** `retries` on the replay path (default 0), a `flaky` marker when a flow fails-then-passes (kept additive rather than a new `RunVerdict`, since existing ternaries fold non-pass into 'uncertain' and would redden CI for an eventually-passing flow), and `.spike-quarantine.json`. Coordinator wired `isQuarantined` into the suite runner — quarantined flows still RUN and REPORT, they are only excluded from the aggregate exit code. 43/43 (v43).
- [x] **(A12, P1)** Build a suite concept over `replay --all`: ordering, tags/filters, `beforeAll`/`afterAll`, shared fixtures (incl. the A6 auth fixture), sharding (`--shard 1/4`), and machine-readable JUnit XML + JSON suite output.
  - **DONE 26-08-09.** `src/suite/{config,runner,reporters}.ts` — `spike.suite.json` (zod, `.strict()`), deterministic default ordering (sorted, not readdir), worker-pool `--workers N` over an INJECTED `runOne` (no engine/ports import, so the Playwright-isolated runner drops in later), `--tag`/`--filter`/`--shard i/N`, JUnit XML + JSON reporters. `replay --all` rewired; exit codes and the `--json` array shape preserved exactly. 84/84 (v44).
- [x] **(A13, P1)** Add network interception via Playwright's `context.route()` (verified working in the spike): block third-party/analytics for speed, force error states deterministically for negative tests, and assert on requests. Add viewport/device/network-throttle emulation alongside.
  - **DONE 26-08-09.** `routeRules` (block / fail-with-status) via CDP `setBlockedURLs` + `Fetch.requestPaused`, plus viewport/device/network-throttle emulation. Config + env driven (`SPIKE_ROUTE_RULES`, `SPIKE_BLOCK_HOSTS`, `SPIKE_VIEWPORT`, `SPIKE_NETWORK_THROTTLE`), applied transport-agnostically via `cdpClient()`.
- [x] **(A14, P1)** Make hand-authored scripts a supported workflow: zod-validate `QaScript` on load (replace the bare `JSON.parse(...) as QaScript` at `script.ts:262`; zod already a dependency), ship `spike validate <script>`, publish a JSON-schema file for editor autocomplete, and document the format.
  - **DONE 26-08-09.** `src/recorder/schema.ts` (zod, `.strict()`, reuses `ScriptRunnerStepSchema`, compile-time drift guard both directions) + `loadScript()` gate. 22/22 (v37); v32 script-runner 24/24 unaffected; recorder round-trip verified via real `scriptFromReport` output.
- [x] **(A15, P1)** Stand up CI: a GitHub Actions workflow running `typecheck`, `test:e2e` against the fixture, and (once A1 lands) `spike replay --all --json` as the regression gate; wire an aggregate `npm test` that runs the `test/` suite files.
  - **DONE 26-08-09.** `npm test` (30 fast suites, pooled) and `npm run test:browser` (28 Chrome-driving suites, forced SERIAL because they bind fixed ports). GitHub Actions runs typecheck+build+fast tests on push/PR; the browser job is `workflow_dispatch`-only with an inline explanation of why a stock runner cannot supply branded Chrome, Nano's model, or live credentials. `docs/infra/testing.md` updated, including that both e2e suites were failing silently until the readOnly fix — the motivating case for having a gate at all.
- [x] **(A16, P1)** Resolve the `.spec.ts` twin honestly — decide: either make it runnable (optional `@playwright/test` peer + emitted config + real `expect()` calls + placeholder resolution; note this is functionally a partial reversal of the DROPPED Playwright-helper decision, `2026-07-07-passmark-gap-implementation-tasks.md:225`) or demote it to a clearly-labelled human-readable summary. Status quo (unrunnable file that looks runnable) is the one wrong option.
  - **DONE 26-08-09.** Took the non-conflicting option: the `.spec.ts` twin is now honestly framed as a non-runnable reference translation (header states what's missing — deps, config, unresolved placeholders, comment-only steps), and the misleading `expect(body).toBeVisible()` no-op is replaced with `// TODO(assertion): <expectation>`. Does NOT re-open the DROPPED Playwright-library decision. `docs/modules/recorder.md` corrected (it claimed `toBeVisible()`, already stale).
- [x] **(A17, P1)** Fix the step budget: set `cfg.maxSteps` to the documented 40 (`config.ts:125`) — per-goal stays 12 automatically via `Math.min(40, DEFAULT_PER_GOAL_STEPS)` (`loop.ts:379`); also fix the independent hardcoded 12 in the lite path (`lite-engine.ts:185`) and reconcile CLAUDE.md.
  - **DONE 26-08-09.** `config.ts` maxSteps 12→40; `lite-engine.ts:185` independent hardcoded 12→40. Per-goal stays 12 via `Math.min(40, DEFAULT_PER_GOAL_STEPS)`. CLAUDE.md text was already correct.
- [x] **(A18, P2)** Treat 4xx as a failure signal: widen `console-network.ts:54` beyond `>= 500` (or add a distinct `clientError` flag) so 400/401/403/404 reach the model's evidence (`planner-prompt.ts:31-35` currently filters them out entirely) and the batch-abort check (`loop.ts:334-337`).
  - **DONE 26-08-09.** `clientError` added to `NetworkEntry` (deliberately separate from `failed`, which drives batch-abort). `firstError()` surfaces 4xx at lowest priority; `networkLines` no longer filters them out, labels `net[4xx]:` distinctly. 14/14 (v36).
- [x] **(A19, P2)** Reduce AX-tree cost on dense pages: region-focused serialization (subtree under the current goal's landmark) and an adaptive `MAX_CHARS` cap (`axtree.ts:12`) so the navigator stops planning against truncated views. (Full incremental-AX patching stays a spike — see enhancement 15.)
  - **DONE 26-08-09.** `serializeAxTree()` extracted with an optional `focus` hint (by id or role+name) that keeps the focused subtree + ancestor spine at full fidelity and collapses the rest, plus a caller-supplied `maxChars` budget. Purely additive — with no options the output is byte-identical, verified in v46 against an independent re-implementation of the old algorithm. 20/20 (v46). Wiring a focus hint through the loop is left as a follow-up.
- [x] **(A20, P2)** Add rate-limit awareness to the router: retry-with-backoff on 429/transient errors before falling to the next rung (`model-router.ts:299-342`), so bursts don't silently demote every call to a weaker adapter.
  - **DONE 26-08-09.** Retry-with-backoff before falling to the next rung: 429/502/503/504 + connection errors only, 3 attempts, 250/750/2000ms with jitter, hard-capped at 4s total (well under the 130s `LLM_CALL_TIMEOUT_MS`). Honours `Retry-After`/Gemini's `retryDelay`, clamped to the remaining budget. 401/400/schema errors still fail fast. Trace carries `attempts`/`retried`. 33/33 (v40).
- [x] **(A21, P2)** Replace `--heal`'s silent in-place overwrite (`engine.ts:602-624`) with risk-tiered auto-accept: version healed scripts (`name.v2.json`), keep the prior version, emit a reviewable diff — then auto-accept locator-only heals (same steps, same assertions) and quarantine heals that change step count or weaken an assertion, reporting those flows as `needs-review` rather than `pass`. Keeps the loop autonomous without letting a heal launder a real regression into green.
  - **DONE 26-08-09.** `classifyHeal()` in `src/recorder/heal-policy.ts` — pure structural diff, no model call. Locator-only drift → `auto`; nav/wait insertions → `notice`; any step removed or assertion removed/weakened (`exact`→`contains`)/re-targeted → `quarantine`, with an unexplained mismatch defaulting to quarantine. A quarantined heal leaves the ORIGINAL script untouched, writes `<name>.candidate.json`, and returns `uncertain` — never `pass`. 43/43 (v43).
- [x] **(A28, P1)** ~~Investigate a click on a correctly-resolved button that had no effect~~ — **ROOT-CAUSED AND FIXED 26-08-09 (wave 3).** Not a descriptor bug (my first filing) and not a one-off (my second): enabling the action cache made `captureActionEffectState` call `browser.axTree()` before the last action of a batch, and `axTree()` deliberately REBINDS the planner's `n`-ids. So in `[type n5, type n7, click n8]` the click resolved `n8` against a snapshot taken after both types — a different node. Fixed by `BrowserPort.peekAxTree()`, an observer snapshot that does not rebind. Reproduced and confirmed via the recorder e2e going 11/19 → 19/19 with a clean 8-step script. Lesson recorded: I twice reasoned from the navigator's `thought` field, which is model narrative generated after the fact, not evidence.
- [x] **(A22, P2)** Capture failure-step evidence: take a screenshot on any step that fails (not only `assert_visual`/`finish` — `loop.ts:512,967,1267`) so a red run in a big suite is debuggable without a re-run.
  - **DONE 26-08-09.** `captureFailureShot()` in `loop.ts` — a step that fails now captures a screenshot immediately, so a red run in a large suite is debuggable without re-running it. Best-effort by construction (never turns a step failure into a crash) and never overwrites an existing shot.

## Suggested enhancements

- [x] `waitFor`/actionability as a first-class `BrowserPort` primitive (implementation vehicle for A4)
- [x] Real assertion vocabulary (implementation vehicle for A5)
- [x] Parallel isolated runs — dynamic ports, `--workers`, Nano per-session or locked (vehicle for A3)
- [x] Storage/session state capture + suite-level auth fixture (vehicle for A6)
- [x] Deterministic-first defaults: flip `actionCache` on (after A9) and split the CLI into `spike author <task>` (AI, once) vs `spike test [suite]` (deterministic, $0, parallel, CI-shaped)
- [x] Tighten `verifyActionEffect` to intent-specific proof (vehicle for A9)
- [x] Headless for the deterministic path, after the Nano port-split prerequisite (vehicle for A7)
- [x] Zod-validated hand-authored script workflow + `spike validate` (vehicle for A14)
- [x] Network interception + emulation (vehicle for A13)
- [x] Flake control: retries, `flaky` verdict, quarantine, regression-vs-flake reporting (vehicle for A11)
- [x] Locator strategy upgrade: testid-first, candidate-locator stacks, scored disambiguation (vehicle for A8)
- [x] Fix or retire the `.spec.ts` twin — deliberate choice between partial reversal of the DROPPED decision and demotion to summary (vehicle for A16)
- [x] Suite orchestration: ordering, tags, fixtures, sharding, JUnit/JSON output (vehicle for A12)
- [x] Raise `maxSteps` to 40, decouple per-goal budget, consider adaptive budgets (vehicle for A17)
- [ ] Incremental AX snapshots — exploratory spike only; relies on experimental CDP `Accessibility.nodesUpdated`; the safer near-term win is region-focused serialization (A19)
  - **DELIBERATELY NOT DONE.** The audit downgraded this to an exploratory spike: it rests on CDP `Accessibility.nodesUpdated`, which is experimental, and would mean maintaining a live AX graph client-side. The safe half of A19 (region-focused serialization + adaptive budget) shipped instead. Left open on purpose, not overlooked.
- [x] Treat 4xx as assertable failure signal; expose console/network failures as assertable conditions, not prompt evidence only (vehicle for A18 + part of A5)
- [x] Heal with a review gate: versioned scripts, reviewable diff, `--accept` (vehicle for A21)
- [x] Benchmark harness over the fixture app: per-step latency split (model/CDP/sleep), suite wall-clock at N workers, replay-vs-AI ratio, flake rate over 20 consecutive runs
  - **DONE 26-08-09.** `npm run bench` (`scripts/benchmark.mjs`) — AI-pass wall-clock split into model vs non-model, per-role call counts, replay median + speedup, and a flake check over N identical replays. Deliberately excluded from `npm test`/CI because the AI leg spends real tokens. **Its own first run produced a bogus 264× speedup** — it inherited the `readOnly: true` default, so the AI pass recorded nothing and the replay leg timed an empty suite; it now forces `SPIKE_READ_ONLY=0` and refuses to report a speedup when no script exists. Results written up in `docs/benchmark.md`.
- [x] Dogfood to a real recorded suite: `readOnly` off for the fixture, genuine pass, commit `generated-tests/*.json` as this repo's regression suite (vehicle for A1)
- [x] App model + coverage ledger — `spike map` / `spike coverage`, the automated→autonomous step (vehicle for A23)
- [x] Invariant oracle tier — zero-human-input assertions on every step, then differential + metamorphic tiers (vehicle for A24)
- [x] Change-triggered re-exploration — app-model fingerprint diff → targeted authoring on new surface only (vehicle for A25)
- [x] Decide the Core-mode engine — spike `connectOverCDP` context isolation, then commit to Playwright or hand-built primitives (vehicle for A26)
- [x] Make lite mode's AI split explicit — cheap vision navigator default, Sonnet brain, Nano opt-in Experimental, honest Settings display (vehicle for A27)
- [x] Risk-tiered heal acceptance — auto-accept locator-only, quarantine assertion changes as `needs-review` (vehicle for A21 reframed)
