> Source audit: [26-08-08-audit-deterministic-speed](./26-08-08-audit-deterministic-speed.md)
> Updated: 26-08-08 · 1/53 done

- [ ] **(A23, P0)** Build the app model + coverage ledger: a discovery pass enumerating reachable routes/states (crawl + link/form extraction, seeded from the app's router manifest where available), persisted as an app model, plus a coverage ledger (routes seen/exercised, elements touched, flows generated) the planner queries to pick what to test next. Entry points: `spike map <url>`, `spike coverage`. Without this, "autonomous full-project QA" is a human writing 200 task strings.
- [ ] **(A24, P0)** Implement the invariant oracle tier: assertions requiring zero human input, run on every step of every flow — no uncaught page errors, no 4xx/5xx (pairs with A18), no layout overflow, no broken images, no empty required regions, optional a11y and response-budget checks. Then layer a differential tier (baseline store, shared with A11) and metamorphic relations (cart count, sort membership, pagination).
- [ ] **(A1, P0)** Exercise the deterministic pillar end-to-end for the first time: run the fixture app with `readOnly` off (`SPIKE_READ_ONLY=0`), get a genuine `pass`, confirm `generated-tests/<slug>.json` + `.spec.ts` are written, then `spike replay` the script and confirm a $0 zero-planner verdict. Commit the resulting script as this repo's own regression suite.
- [ ] **(A2, P0)** Attack the 5.4s-per-3-actions model-latency floor: make deterministic-first the default execution order (cache → script → replay → navigator), so a model call only happens when no deterministic source can supply the next action. (Depends on A9 tightening; pairs with enhancement items 5–6.)
- [ ] **(A3, P0)** Remove the concurrency singletons using `PlaywrightBrowser`'s per-run `BrowserContext` (verified isolated for cookies + localStorage) instead of one shared profile: one context per worker, `--workers N` on `replay --all`, and a per-run artifacts dir. Still needed alongside: the Nano runner becomes per-session or a properly locked shared service (it is a separate HTTP port + tab, outside Playwright's contexts), and the VibeService single-run lock (`vibe/service.ts:94,119`) stays deliberate for the panel.
- [ ] **(A4, P0)** Add `waitFor`/actionability + `waitForIdle` primitives to `BrowserPort` and delete every fixed `sleep()` in `cdp-browser.ts` (~20 sites) and `replay.ts:215`. `Network.enable` is already on (`console-network.ts:22`) so network-idle needs no new CDP domain. Target: replace ~1,150ms of fixed sleep per 3-action batch with condition-based waits.
- [ ] **(A5, P0)** Build a real assertion vocabulary alongside substring `assert_dom`: `assert_text {exact|contains|regex}`, `assert_count`, `assert_url`, `assert_attr`, `assert_network {urlPattern, status}`, `assert_no_console_errors`, `assert_visible/hidden/enabled/disabled` — wired through actions.ts schemas, the loop executor, the recorder, and replay.
- [x] **(A26, P1)** ~~Decide the Core-mode engine~~ — **RESOLVED 2026-08-08.** Spike (`docs/plan/spikes/a26-connect-over-cdp.mjs`) passed 9/9: `newContext()` over `connectOverCDP` gives real cookie + localStorage isolation, concurrent driving, clean `storageState()`, and working `route()`, without disturbing the daemon's Chrome. **Decision: A3/A6/A13 via Playwright `connectOverCDP` as a `PlaywrightBrowser` implementation of `BrowserPort`; A4 hand-built in the port layer so lite mode gets it too.** `playwright-core` = 1 package, no browser download.
- [ ] **(A26, P1)** Implement `PlaywrightBrowser` — a third `BrowserPort` implementation attaching via `chromium.connectOverCDP()` to the Chrome the daemon already launches (Core mode only; lite keeps `ExtensionBrowser`). Add `playwright-core` as a dependency. This is the vehicle for A3, A6, and A13 below — do it before them.
- [ ] **(A27, P1)** Make lite mode's AI split explicit and correct: change the default navigator from the no-op `nano:ondevice` pin (`settings-data.ts:73`, which `lite-engine.ts:88-91` documents as "not a plan-step candidate") to a cheap vision API model (`claude-haiku-4-5` / `gemini-3-flash-preview`, already in `NAVIGATOR_MODELS` `settings-data.ts:88-92`); keep the brain on `claude:api` Sonnet; demote Nano to an opt-in Experimental navigator; make the Settings panel display what is actually running. Panel copy states the contract: one API key, a small model drives, a big model judges.
- [ ] **(A25, P1)** Add change-triggered re-exploration: fingerprint the app model per run (route set + per-route AX signature), diff against the previous run, and trigger targeted AI authoring only for new or changed surface — so the expensive AI pass is spent exactly where determinism has nothing to replay. (Depends on A23's app model.)
- [ ] **(A6, P1)** Add storage-state capture/injection via Playwright's `context.storageState()` / `newContext({storageState})` (verified working in the spike), plus a suite-level auth fixture that logs in once and injects state into every subsequent flow. Kills ~36–40 min of repeated UI login per 200-flow suite pass. Keep a raw-CDP fallback (`Network.getCookies`/`setCookies` through `cdpClient()`, `cdp-browser.ts:70-72`) for the extension transport.
- [ ] **(A7, P1)** Make headless reachable: add `--headless` for the replay path, gated on the run not needing Nano (`replay.ts:53-55`). Prerequisite: split the Nano runner onto its own port/Chrome first (slice of A3) — today whoever launches the shared Chrome first wins the headless choice and would silently drag Nano headless (`engine.ts:188-189,224`, `launch.ts:44`).
- [ ] **(A8, P1)** Upgrade locator strategy: `data-testid`-first resolution with role+name fallback, teach the recorder to prefer testids, record a stack of candidate locators per step, and replace the ambiguity hard-fail (`replay.ts:417-422`) with scored disambiguation (position, nearest landmark, sibling text) before giving up.
- [ ] **(A9, P1)** Tighten `verifyActionEffect`: remove the `changes.length` catch-all (`action-cache.ts:335`) and require intent-matching proof per action type (a click must produce a targeted change — target's own state, new URL, or named-region mutation — not merely "the page changed"). Then flip `actionCache` default to true (`config.ts:123`).
- [ ] **(A10, P1)** Fix replay matching failure modes: surface near-miss scores at the CLI when no script clears the 0.62 threshold (`matcher.ts:29`); make the silent replay-fail→fresh-AI fallback loud (`engine.ts:429-436`), including in `--json` mode; content-hash script names to kill the 60-char slug-collision overwrite (`script.ts:82-90`, `saveScript :237-245`).
- [ ] **(A11, P1)** Add flake control: per-flow `retries: N`, a `flaky` verdict when fail-then-pass on retry, a quarantine list excluded from the exit code, and a suite report separating regressions from flakes.
- [ ] **(A12, P1)** Build a suite concept over `replay --all`: ordering, tags/filters, `beforeAll`/`afterAll`, shared fixtures (incl. the A6 auth fixture), sharding (`--shard 1/4`), and machine-readable JUnit XML + JSON suite output.
- [ ] **(A13, P1)** Add network interception via Playwright's `context.route()` (verified working in the spike): block third-party/analytics for speed, force error states deterministically for negative tests, and assert on requests. Add viewport/device/network-throttle emulation alongside.
- [ ] **(A14, P1)** Make hand-authored scripts a supported workflow: zod-validate `QaScript` on load (replace the bare `JSON.parse(...) as QaScript` at `script.ts:262`; zod already a dependency), ship `spike validate <script>`, publish a JSON-schema file for editor autocomplete, and document the format.
- [ ] **(A15, P1)** Stand up CI: a GitHub Actions workflow running `typecheck`, `test:e2e` against the fixture, and (once A1 lands) `spike replay --all --json` as the regression gate; wire an aggregate `npm test` that runs the `test/` suite files.
- [ ] **(A16, P1)** Resolve the `.spec.ts` twin honestly — decide: either make it runnable (optional `@playwright/test` peer + emitted config + real `expect()` calls + placeholder resolution; note this is functionally a partial reversal of the DROPPED Playwright-helper decision, `2026-07-07-passmark-gap-implementation-tasks.md:225`) or demote it to a clearly-labelled human-readable summary. Status quo (unrunnable file that looks runnable) is the one wrong option.
- [ ] **(A17, P1)** Fix the step budget: set `cfg.maxSteps` to the documented 40 (`config.ts:125`) — per-goal stays 12 automatically via `Math.min(40, DEFAULT_PER_GOAL_STEPS)` (`loop.ts:379`); also fix the independent hardcoded 12 in the lite path (`lite-engine.ts:185`) and reconcile CLAUDE.md.
- [ ] **(A18, P2)** Treat 4xx as a failure signal: widen `console-network.ts:54` beyond `>= 500` (or add a distinct `clientError` flag) so 400/401/403/404 reach the model's evidence (`planner-prompt.ts:31-35` currently filters them out entirely) and the batch-abort check (`loop.ts:334-337`).
- [ ] **(A19, P2)** Reduce AX-tree cost on dense pages: region-focused serialization (subtree under the current goal's landmark) and an adaptive `MAX_CHARS` cap (`axtree.ts:12`) so the navigator stops planning against truncated views. (Full incremental-AX patching stays a spike — see enhancement 15.)
- [ ] **(A20, P2)** Add rate-limit awareness to the router: retry-with-backoff on 429/transient errors before falling to the next rung (`model-router.ts:299-342`), so bursts don't silently demote every call to a weaker adapter.
- [ ] **(A21, P2)** Replace `--heal`'s silent in-place overwrite (`engine.ts:602-624`) with risk-tiered auto-accept: version healed scripts (`name.v2.json`), keep the prior version, emit a reviewable diff — then auto-accept locator-only heals (same steps, same assertions) and quarantine heals that change step count or weaken an assertion, reporting those flows as `needs-review` rather than `pass`. Keeps the loop autonomous without letting a heal launder a real regression into green.
- [ ] **(A22, P2)** Capture failure-step evidence: take a screenshot on any step that fails (not only `assert_visual`/`finish` — `loop.ts:512,967,1267`) so a red run in a big suite is debuggable without a re-run.

## Suggested enhancements

- [ ] `waitFor`/actionability as a first-class `BrowserPort` primitive (implementation vehicle for A4)
- [ ] Real assertion vocabulary (implementation vehicle for A5)
- [ ] Parallel isolated runs — dynamic ports, `--workers`, Nano per-session or locked (vehicle for A3)
- [ ] Storage/session state capture + suite-level auth fixture (vehicle for A6)
- [ ] Deterministic-first defaults: flip `actionCache` on (after A9) and split the CLI into `spike author <task>` (AI, once) vs `spike test [suite]` (deterministic, $0, parallel, CI-shaped)
- [ ] Tighten `verifyActionEffect` to intent-specific proof (vehicle for A9)
- [ ] Headless for the deterministic path, after the Nano port-split prerequisite (vehicle for A7)
- [ ] Zod-validated hand-authored script workflow + `spike validate` (vehicle for A14)
- [ ] Network interception + emulation (vehicle for A13)
- [ ] Flake control: retries, `flaky` verdict, quarantine, regression-vs-flake reporting (vehicle for A11)
- [ ] Locator strategy upgrade: testid-first, candidate-locator stacks, scored disambiguation (vehicle for A8)
- [ ] Fix or retire the `.spec.ts` twin — deliberate choice between partial reversal of the DROPPED decision and demotion to summary (vehicle for A16)
- [ ] Suite orchestration: ordering, tags, fixtures, sharding, JUnit/JSON output (vehicle for A12)
- [ ] Raise `maxSteps` to 40, decouple per-goal budget, consider adaptive budgets (vehicle for A17)
- [ ] Incremental AX snapshots — exploratory spike only; relies on experimental CDP `Accessibility.nodesUpdated`; the safer near-term win is region-focused serialization (A19)
- [ ] Treat 4xx as assertable failure signal; expose console/network failures as assertable conditions, not prompt evidence only (vehicle for A18 + part of A5)
- [ ] Heal with a review gate: versioned scripts, reviewable diff, `--accept` (vehicle for A21)
- [ ] Benchmark harness over the fixture app: per-step latency split (model/CDP/sleep), suite wall-clock at N workers, replay-vs-AI ratio, flake rate over 20 consecutive runs
- [ ] Dogfood to a real recorded suite: `readOnly` off for the fixture, genuine pass, commit `generated-tests/*.json` as this repo's regression suite (vehicle for A1)
- [ ] App model + coverage ledger — `spike map` / `spike coverage`, the automated→autonomous step (vehicle for A23)
- [ ] Invariant oracle tier — zero-human-input assertions on every step, then differential + metamorphic tiers (vehicle for A24)
- [ ] Change-triggered re-exploration — app-model fingerprint diff → targeted authoring on new surface only (vehicle for A25)
- [ ] Decide the Core-mode engine — spike `connectOverCDP` context isolation, then commit to Playwright or hand-built primitives (vehicle for A26)
- [ ] Make lite mode's AI split explicit — cheap vision navigator default, Sonnet brain, Nano opt-in Experimental, honest Settings display (vehicle for A27)
- [ ] Risk-tiered heal acceptance — auto-accept locator-only, quarantine assertion changes as `needs-review` (vehicle for A21 reframed)
