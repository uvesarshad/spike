# Tasks — Non-Technical Onboarding

> Source audit: [26-07-14-audit-non-tech-onboarding](./26-07-14-audit-non-tech-onboarding.md)
> Updated: 26-07-14 · 13/16 done

- [ ] **(A1, P0)** Verify the npm package `browser-qa-subagent` is actually published under that exact name (the one-liner and `sw.js` both `npm i -g` it). If not, publish it or fix every reference. — **VERIFIED 2026-07-14: `npm view` → 404, NOT PUBLISHED. Remediation (publish, or repoint every reference) is still outstanding — needs npm publish rights.**
- [ ] **(A1, P0)** Verify `github.com/uvesarshad/browser-qa-subagent` is public and serves `install/install.ps1` + `install/install.sh` at `main` (the panel's `INSTALL_BASE`, `panel.js:140`). Fix the repo/path if the raw URL 404s. — **VERIFIED 2026-07-14: raw URL → HTTP 404 (repo private/missing/renamed). The daemon one-liner is non-functional until this is public. Outstanding.**
- [x] **(A1, P0)** Add both distribution preconditions above to the plan's "Definition of done" so they gate any onboarding ship. — *Added to `2026-07-13-non-tech-onboarding.md` Definition of done.*
- [x] **(A2, P0)** De-risk Web Store approval of the `debugger` + `<all_urls>` combo BEFORE building store assets: draft the permission justifications and, if possible, a pre-review inquiry. Treat rejection as a live hypothesis, not a form-filling step. — *Reviewer-facing justifications for every permission drafted in `2026-07-14-store-readiness.md` §1, with honest rejection-risk framing. (Actual submission/pre-review is an external step.)*
- [x] **(A2, P0)** Add the persistent "…started debugging this browser" CDP infobar to onboarding copy (and a GIF) so non-tech users expect it instead of reading it as "something's wrong." — *First-run / in-run / tooltip copy drafted in `2026-07-14-store-readiness.md` §2. (GIF asset still to produce.)*
- [x] **(A2, P0)** Document the fallback if `debugger` is rejected: without a non-`debugger` transport the Store path collapses to developer-only unpacked, which defeats the plan — decide the contingency now. — *Contingency + ordered fallback plan in `2026-07-14-store-readiness.md` §3.*
- [x] **(A4, P1)** Add a bridge protocol-version handshake on connect; when the daemon is older than the panel needs, show "update the desktop app: re-run the one-liner" instead of a green-dot-but-broken run. — *Implemented: `PROTOCOL_VERSION` in `src/bridge/bridge-server.ts`, `bridge.hello` handshake + 4s timeout in `sw.js`, amber `dot-warn` state + update banner in panel; `bridgeHealthy()` now gates auto-fix/clip/run so an outdated daemon can't silently pass.*
- [x] **(A5, P1)** Add a visible per-run spend estimate/meter in the panel and an optional monthly cap (uncapped autonomous agent + pasted key is the audience's biggest hidden risk). — *`report.spendSummary` {freeCalls, paidCalls, totalTokens, estimatedUsd, capUsd?} flows through `slimReport`→done payload; panel renders a "Spend: ~$X est." line; `spendCapUsd` setting + `QA_SPEND_CAP_USD` aborts the run when exceeded (per-run cap; estimate-only proxy at $3/1M tok).*
- [x] **(A5, P1)** Make the first N runs read-only/dry-run by default; require an explicit flip before the agent may click/type, with plain-language copy on what "allow click/type on this site" permits. — *`readOnly` QaSetting defaults TRUE; enforced centrally in `loop.ts` (`isMutatingAction` guard) — skips click/type/upload/drag/blur/mouse/tab/script with a "read-only mode: skipped …" step; panel toggle + `QA_READ_ONLY` env; wired through config/service/lite-engine/engine.*
- [x] **(A3, P1)** Reword the plan's "BYOK + Nano" framing to "BYOK (Nano opportunistically accelerates visual checks when available)". Nano is visual-only and `liteUsable:false` (`lite-engine.ts:129`) and hard-gated, so on a typical machine the pasted cloud key drives everything. — *Reworded throughout `2026-07-13-non-tech-onboarding.md`.*
- [x] **(A6, P1)** Scope the "zero terminal" headline: Lite mode = zero terminal ever; optional desktop app = one terminal paste, once. Only Lite is genuinely terminal-free. — *Headline + Target-flow reworded in the plan.*
- [x] **(A7, P2)** Fix the two stale usage-comment lines `install.ps1:4` and `install.sh:5` (`<your-host>` → real `INSTALL_BASE`). Panel is already correct; reclassify this from "wiring gap" to trivial. — *Both comment lines now show the real `raw.githubusercontent.com/…/install` URL.*
- [x] **(A8, P2)** Add a gating acceptance test to Definition of done: clean-profile, Store-style (non-`loadUnpacked`) install, no local daemon, paste key, run against the fixture, assert a verdict. — *Added to the plan's Definition of done. (Execution is a manual pre-ship step.)*
- [x] **(A10, P2)** Define the privacy-policy CONTENT (page content read, screenshots/clips via `tabCapture`, keys in `chrome.storage`, data sent to third-party model providers), not just the URL — Google reads this closely given `debugger` + `<all_urls>`. — *Disclosure checklist in `2026-07-14-store-readiness.md` §4.*
- [x] **(A9, P2)** Add an origin allow-list or pairing token to the `ws://localhost:9322` bridge before a public launch (broad audience widens the localhost attack surface). — *Implemented: the WS accept path now rejects `http(s)://` page origins (closes the arbitrary-web-page threat); `chrome-extension://` and no-Origin (Node clients) still pass. A pre-shared pairing token is documented as a follow-up (needs config plumbing).*
- [ ] **(Enhancement, P2)** Spike a native signed installer (.exe/.dmg) that bundles Node — it eliminates BOTH remaining daemon frictions (Node prerequisite + terminal paste). Cost it before committing to the one-liner as the permanent answer. — *Scoping notes written in `2026-07-14-store-readiness.md` §5; the actual spike/costing remains to do.*

## Verification (this pass)

- `tsc --noEmit` clean; both bundles build (`dist/` + `extension/lite-engine.js` 442.93 KB); `sw.js`/`panel.js` parse.
- Pure-logic test sweep green: v13.tier4 32/32, v20.tokens 17/17, v29.telemetry 25/25, v33.trace-spans 13/13, v25.providers 34/34, v30.video-assertion 21/21, v31/v32/v28/v26/v27/v21/m4/port-contract all pass — 0 lanes failing.
- One regression found + fixed during integration: adding `spendSummary` to `slimReport` broke `verdictPayloadTokens` accounting (it counted a stale duplicate `slimForEstimate`); fixed by counting the real `slimReport` and deleting the duplicate, and by attaching `spendSummary` before token counting.
- Not yet run (environment-gated, need live Chrome/models): the e2e / live-AI lanes and a real clean-profile Store-style install (task A8).
